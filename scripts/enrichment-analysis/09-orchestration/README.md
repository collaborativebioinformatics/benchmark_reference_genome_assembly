# Orchestration and audit

`01-submit-analysis.sh` submits the dependency-controlled workflow from input
audit through figures. `00-audit-results.*` performs the final version, row,
mask, enrichment, cluster, and figure checks.
