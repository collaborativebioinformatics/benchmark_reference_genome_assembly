# SV liftOver

`01-lift-callset-to-hs1.*` projects each non-hs1 callset with liftoverSV and
canonicalizes output with `00-canonicalize-hs1-sv.py`. `02-audit-liftover.*`
checks record accounting and projected coordinates.
