# Configuration

These tables define DNANexus inputs, the 14 callsets, benchmark parameters,
annotation sources, exclusions, and the plotting environment. Update paths and
object IDs here rather than editing analysis code.
