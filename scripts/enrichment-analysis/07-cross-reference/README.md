# Cross-reference comparison

Stage 00 prepares and collapses strict or endpoint-relaxed callsets into
SV-aware clusters. Stage 01 annotates clusters and calculates O/E enrichment for
each exact 1/7 through 7/7 sharing class.
