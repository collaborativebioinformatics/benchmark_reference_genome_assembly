# Common setup

`00-library.sh` defines shared paths and task loading. `01-install-tools.*`
creates the benchmark environment and installs the pinned liftoverSV checkout.
`environment.yml` specifies the analysis packages.
